# IM-ON 모바일 리디자인 표준

**상태:** 승인된 디자인 기준  
**버전:** 1.0  
**기준일:** 2026-07-27  
**선택 방향:** 목업 3 `Soft Blue Glass`

## 1. 문서 목적과 우선순위

이 문서는 IM-ON 현장 출결 웹앱을 모바일 중심으로 리디자인할 때 적용할 시각 언어, 화면 구조, UI 컴포넌트, 상호작용 상태와 기능 보호 경계를 정의한다.

시각 기준은 다음 파일이다.

- 비교 화면: [`docs/tmp/index.html`](docs/tmp/index.html)
- 선택 목업: [`docs/tmp/mockup-3-soft-blue-glass.html`](docs/tmp/mockup-3-soft-blue-glass.html)
- 목업 스타일: [`docs/tmp/assets/mockups.css`](docs/tmp/assets/mockups.css)
- 브랜드 로고 기준: [`docs/tmp/assets/juyon-snc-logo.png`](docs/tmp/assets/juyon-snc-logo.png)

기능 기준은 다음 문서가 우선한다.

1. [`CURRENT_STANDARD.md`](CURRENT_STANDARD.md)
2. [`IMPLEMENTATION_AUDIT.md`](IMPLEMENTATION_AUDIT.md)
3. 이 문서
4. `archive/project-docs/` 아래의 과거 문서

이 문서와 `CURRENT_STANDARD.md`가 충돌하면 리디자인을 중단하고 운영 기준을 먼저 확인한다. 이 문서는 표현 계층을 규정하며 출결, 권한, 시간, 위치, 저장소 규칙을 변경하지 않는다.

## 2. 디자인 결정

### 2.1 방향

`Soft Blue Glass`는 로고의 네이비를 핵심 색으로 사용하고, 옅은 블루 배경과 반투명 흰색 표면을 결합한다.

핵심 인상은 다음과 같다.

- 신뢰할 수 있는 현장 운영 도구
- 모바일에서 빠르게 이해되는 단순한 위계
- 차갑지 않은 네이비와 부드러운 블루
- 장식보다 상태와 다음 행동이 먼저 보이는 구성
- 관리자도 표보다 목록을 우선하는 모바일 구조

### 2.2 목표

- 사용자가 현재 상태와 다음 출결 행동을 첫 화면에서 파악한다.
- 주요 출결 버튼을 한 손 엄지 영역에서 누를 수 있다.
- 관리자는 작은 화면에서도 미완료 인원과 현재 시간대를 빠르게 확인한다.
- 모든 부서, 역할, 출결 이벤트의 기존 동작을 유지한다.
- UI 기본 요소의 키보드 조작, 포커스 관리와 접근성을 개선한다.

### 2.3 비목표

- 인증 또는 권한 모델 변경
- 출결 가능 여부와 서버 검증 규칙 변경
- GPS 및 출입 구역 판정 변경
- DB, Supabase 스키마 또는 Google Sheet 운영 규칙 변경
- 새로운 부서, 근무조 또는 출결 이벤트 추가
- 리디자인을 이유로 한 대규모 비즈니스 로직 리팩터링

## 3. 기술 및 UI 라이브러리 기준

### 3.1 설치된 라이브러리

| 패키지 | 설치 버전 | 용도 |
| --- | --- | --- |
| `radix-ui` | `1.6.7` | 접근 가능한 비스타일드 인터랙션 프리미티브 |
| `lucide-react` | `1.27.0` | 일반 UI 아이콘 |
| `clsx` | 기존 설치 유지 | 조건부 클래스 조합 |

공식 참고 문서:

- Radix Primitives: <https://www.radix-ui.com/primitives>
- Radix 접근성: <https://www.radix-ui.com/primitives/docs/overview/accessibility>
- Lucide React: <https://lucide.dev/guide/react>

### 3.2 Radix 적용 원칙

Radix는 스타일 소스가 아니라 접근성과 상호작용 기반으로만 사용한다. 색상, 여백, 반경, 타이포그래피는 이 문서의 토큰과 프로젝트 CSS가 담당한다.

허용하는 1차 적용 대상은 다음과 같다.

- `Dialog`, `AlertDialog`: 동의서 상세, 정정 확인, 비활성화 확인
- `Accordion`, `Collapsible`: 오늘 기록, 상세 순서, 설정 그룹
- `Popover`, `Select`: 관리자 필터와 선택 UI
- `Tooltip`: 아이콘 전용 버튼 설명
- `Checkbox`, `Switch`, `RadioGroup`: 설정 상태와 선택 그룹

다음 원칙을 지킨다.

- 필요한 프리미티브만 하위 경로로 가져온다.
- 기존 `value`, `name`, `id`, `onChange`, `onClick`, `onSubmit` 계약을 보존한다.
- Dialog에는 항상 접근 가능한 제목과 설명을 제공한다.
- Portal이 지도, 하단 고정 버튼, 다른 오버레이의 쌓임 순서를 깨지 않게 검증한다.
- Radix의 내부 상태를 출결이나 서버 상태의 진실 공급원으로 사용하지 않는다.
- `Toast`는 1차 적용에서 제외하고 현재의 인라인 성공·오류 메시지를 유지한다.

### 3.3 Radix를 사용하지 않는 영역

- 관리자 구역 전환은 서버 URL 상태이므로 Radix `Tabs`로 바꾸지 않는다.
- `/admin`, `/admin?section=users`, `/admin?section=accounts`, `/admin?section=system` 링크 계약을 유지한다.
- `time-wheel-picker`의 값 처리와 시간 창 계산을 다른 Date/Time 컴포넌트로 교체하지 않는다.
- Kakao Map과 위치 선택 인터랙션을 Radix 컴포넌트로 감싸서 동작을 바꾸지 않는다.
- 출결 주요 버튼의 비동기 상태는 기존 `AttendanceActionPanel`이 계속 소유한다.

### 3.4 Lucide 적용 원칙

- 일반적인 탐색, 설정, 상태 보조 아이콘에만 사용한다.
- named import를 사용하고 동적 전체 아이콘 로더는 사용하지 않는다.
- 기본 크기는 `18px`, 하단 내비게이션은 `20px`, 단독 주요 아이콘은 최대 `24px`이다.
- 장식 아이콘은 `aria-hidden="true"`로 처리한다.
- 아이콘 전용 버튼은 실제 버튼 요소와 명확한 `aria-label`을 사용한다.
- 색상만으로 상태를 전달하지 않고 항상 텍스트를 함께 표시한다.
- 주연SNC 로고, Kakao 브랜드 자산, 지도 마커는 Lucide로 대체하지 않는다.

### 3.5 도입하지 않는 UI 체계

1차 리디자인에는 Tailwind CSS, shadcn/ui, MUI, Ant Design, Chakra UI, Mantine을 추가하지 않는다. 기존 전역 CSS와 동시에 여러 시각 체계를 운용하지 않는다.

## 4. 기능 보호 경계

### 4.1 변경 금지 영역

다음 영역은 별도 기능 요구와 승인이 없는 한 리디자인에서 수정하지 않는다.

- `lib/auth.ts`, `lib/auth-config.ts`, `lib/session-token.ts`
- `lib/attendance-rules.ts`, `lib/attendance-events.ts`, `lib/current-period.ts`
- `lib/app-data.ts`, `lib/supabase-store.ts`, `lib/google-sheets.ts`
- `app/api/**`
- `supabase/**`
- 쿠키, 세션, 역할 게이트
- 시간대 `Asia/Seoul`과 업무일 경계
- 출결 이벤트 저장 형식
- Google Sheet 명단 읽기·쓰기 범위

### 4.2 부서 기능 계약

| 부서 | 표시해야 하는 출결 단계 |
| --- | --- |
| `memory_pcs` | 출근, 오전 TBM, 점심 등록, 점심 출문, 점심 입문, 오후 TBM, 퇴근 TBM, 퇴근 |
| `memory` | 출근, 퇴근 중심; 점심/TBM 숨김 |
| `foundry_pcs` | 출근, 퇴근 중심; 점심/TBM 숨김 |

진행률의 분모는 부서에 실제 노출되는 단계 수를 사용한다. `memory_pcs`만 `0/8`이며 다른 부서에 `8`을 고정하지 않는다.

### 4.3 역할 계약

| 역할 | 화면 범위 |
| --- | --- |
| `master` | 전체 부서 관리 |
| `admin` | 본인 부서 수정 |
| `sub_admin` | 본인 부서 조회 |
| `user` | 본인 출결 사용 |

UI에서 숨김 처리를 하더라도 서버 권한 검증을 대체하지 않는다. `sub_admin`에게 현재 허용되지 않은 관리 기능을 새 UI가 노출하지 않는다.

### 4.4 상태 결정 계약

- UI는 서버에서 받은 가능 여부와 사유를 표시한다.
- 클라이언트가 출결 가능 시간이나 구역 규칙을 별도로 재구현하지 않는다.
- 성공 화면은 서버 저장 성공 응답 이후에만 표시한다.
- 위치 수집 실패, 낮은 GPS 정확도, 허용 구역 밖 상태를 구분해 표시한다.
- 중복 요청 방지와 버튼 비활성화 규칙을 유지한다.

## 5. 디자인 토큰

### 5.1 색상

| 의미 | CSS 변수 권장명 | 값 |
| --- | --- | --- |
| 앱 캔버스 | `--color-canvas` | `#EAF1FF` |
| 외부 캔버스 | `--color-canvas-deep` | `#DCE7FB` |
| 글래스 표면 | `--color-surface-glass` | `rgba(255, 255, 255, 0.68)` |
| 강한 표면 | `--color-surface-strong` | `rgba(255, 255, 255, 0.86)` |
| 기본 텍스트 | `--color-text` | `#172044` |
| 보조 텍스트 | `--color-muted` | `#66739B` |
| 경계선 | `--color-line` | `rgba(86, 105, 170, 0.16)` |
| 브랜드 | `--color-brand` | `#3D5EB7` |
| 브랜드 네이비 | `--color-brand-strong` | `#293986` |
| 브랜드 약한 배경 | `--color-brand-soft` | `rgba(92, 119, 206, 0.11)` |
| 브랜드 위 텍스트 | `--color-on-brand` | `#FFFFFF` |
| 성공 | `--color-success` | `#247965` |
| 성공 배경 | `--color-success-soft` | `rgba(72, 174, 145, 0.13)` |
| 주의 | `--color-warning` | `#9E671D` |
| 주의 배경 | `--color-warning-soft` | `rgba(233, 178, 83, 0.16)` |
| 오류 | `--color-danger` | `#B43F55` |
| 오류 배경 | `--color-danger-soft` | `rgba(180, 63, 85, 0.12)` |
| Kakao 버튼 | `--color-kakao` | `#FEE500` |

브랜드 주요 카드의 권장 배경은 다음과 같다.

```css
linear-gradient(145deg, rgba(39, 55, 134, 0.94), rgba(72, 103, 181, 0.88))
```

앱 캔버스의 권장 배경은 다음과 같다.

```css
radial-gradient(circle at 12% 4%, rgba(255, 255, 255, 0.94), transparent 27%),
radial-gradient(circle at 92% 34%, rgba(122, 154, 235, 0.24), transparent 29%),
linear-gradient(160deg, #EEF5FF 0%, #E7EFFF 48%, #DFE9FF 100%)
```

### 5.2 타이포그래피

기본 글꼴은 외부 네트워크 요청 없이 다음 시스템 스택을 사용한다.

```css
"Pretendard Variable", "Pretendard", "Noto Sans KR", system-ui, -apple-system, BlinkMacSystemFont, sans-serif
```

| 용도 | 크기 | 굵기 | 행간 |
| --- | --- | --- | --- |
| 로그인 핵심 문장 | `30–38px` | `800–900` | `1.16` |
| 화면 제목 | `24px` | `800–900` | `1.2` |
| 다음 행동 제목 | `24–27px` | `850–900` | `1.2` |
| 카드 제목 | `14–18px` | `750–850` | `1.35` |
| 본문 | `13–15px` | `400–600` | `1.55–1.7` |
| 메타·캡션 | `10–12px` | `600–800` | `1.4` |

한글 제목은 `letter-spacing: -0.03em`에서 `-0.05em` 범위를 사용하고, 본문은 과도한 음수 자간을 사용하지 않는다.

### 5.3 간격, 반경, 그림자

- 간격 스케일: `4, 8, 12, 16, 18, 20, 24, 32px`
- 작은 컨트롤 반경: `12px`
- 버튼·일반 카드 반경: `16px`
- 글래스 목록 카드 반경: `21px`
- 브랜드 주요 카드 반경: `24px`
- Pill 반경: `999px`
- 글래스 블러: `18px`
- 고정 헤더·하단 바 블러: `22px`
- 기본 그림자: `0 10px 26px rgba(71, 91, 148, 0.08)`
- 주요 카드 그림자: `0 18px 38px rgba(46, 70, 145, 0.20)`

그림자는 정보 위계를 구분하는 용도로만 사용하고 모든 카드에 강한 그림자를 반복하지 않는다.

## 6. 모바일 레이아웃 규칙

### 6.1 기준 크기

- 디자인 기준: `390 × 844px`
- 최소 지원 폭: `320px`
- 주요 검증 폭: `360px`, `390px`, `430px`
- 앱 셸 최대 폭: `430px`
- 최소 높이: `100svh`

모바일에서는 화면 전체 폭을 사용한다. 넓은 화면에서는 앱 셸을 중앙에 배치할 수 있지만 장식용 휴대전화 베젤을 만들지 않는다.

### 6.2 공통 구조

- 헤더 높이는 약 `64px`로 유지한다.
- 본문 좌우 여백은 `16–18px`를 사용한다.
- 주요 출결 버튼은 화면 하단의 엄지 영역에 고정한다.
- 고정 영역은 `env(safe-area-inset-bottom)`을 반영한다.
- 본문에는 하단 고정 영역 높이만큼 여백을 확보한다.
- 어떤 화면에서도 가로 스크롤을 만들지 않는다.
- 관리자 데이터는 모바일에서 가로 표가 아닌 사람 중심 목록으로 표시한다.
- 목업의 `화면 보기` 컨트롤은 검토 전용이며 운영 UI에 포함하지 않는다.

## 7. 화면별 기준

### 7.1 로그인

구성 순서는 다음과 같다.

1. 실제 주연SNC 로고와 서비스명
2. 현장 출결 서비스의 짧은 설명
3. 하단 Kakao 로그인 버튼
4. 초대 링크 안내
5. 운영 상태와 서울 시간 기준 안내

로그인 방식은 관리자 초대 링크와 Kakao 인증을 유지한다. 개발 모드 역할 버튼의 `마스터 입장`, `팀장 입장`, `조장 입장`, `대원 입장` 접근 가능한 이름을 보존한다.

### 7.2 Kakao 등록, 비밀번호 변경, 동의

- 로그인과 같은 앱 셸, 토큰과 글래스 표면을 사용한다.
- 입력값, Server Action, 오류 문구와 리디렉션 계약을 변경하지 않는다.
- 동의 상세는 Radix `Dialog` 적용 후보지만 동의 상태와 제출 동작은 기존 컴포넌트가 소유한다.
- 필수 동의와 선택 동의를 색상만으로 구분하지 않는다.

### 7.3 사용자 출결 화면

첫 화면의 정보 우선순위는 다음과 같다.

1. 날짜, 사용자, 부서와 근무조
2. 현재 상태와 다음 가능 행동
3. 위치 확인 상태
4. 오늘 완료 진행률
5. 하단 주요 행동 버튼

구조 규칙:

- 접근 가능한 화면 제목 `출석체크`를 보존한다.
- 시각적 인사말은 별도의 부제목으로 사용할 수 있다.
- 주요 카드에는 현재 또는 다음 행동 하나만 강조한다.
- 시간 창은 서버에서 받은 유효 설정을 표시한다.
- 위치 상세와 전체 출결 순서는 기본적으로 접을 수 있다.
- 완료 후에는 저장 시간, 다음 단계와 진행률을 함께 갱신한다.
- 출결 단계가 숨겨지는 부서에는 존재하지 않는 단계를 안내하지 않는다.

주요 행동 상태:

| 상태 | 화면 표현 |
| --- | --- |
| 준비 | 브랜드 버튼과 `위치 확인 후 기록` 안내 |
| 위치 확인 | 버튼 비활성화, `위치 확인 중`과 진행 안내 |
| 서버 저장 | 중복 클릭 방지, `기록 중` 안내 |
| 성공 | 성공 색상, 기록 시간, 다음 단계 표시 |
| 잠김 | 잠김 사유와 가능 시간 표시 |
| 위치 오류 | GPS 정확도, 권한 또는 구역 사유와 재시도 안내 |
| 서버 오류 | 기존 서버 오류 문구를 유지하고 재시도 가능 상태로 복귀 |

### 7.4 관리자 화면

하단 내비게이션은 실제 네 구역을 그대로 사용한다.

1. 오늘 현황
2. 근태 관리
3. 계정 관리
4. 시스템 설정

내비게이션은 Next.js `Link`와 현재 URL query를 유지한다. 선택 상태는 URL에서 결정한다.

#### 오늘 현황

- 날짜, 부서, 현재 시간대를 상단에 표시한다.
- 대상, 완료, 미완료를 3개 지표로 요약한다.
- 미완료 또는 확인 필요 인원을 목록 상단에 둔다.
- 모바일에서 기존 가로 표를 그대로 축소하지 않는다.
- 전체 기간 상세는 Drawer/Dialog 또는 별도 접힘 영역으로 제공한다.

#### 근태 관리

- 날짜와 상태 필터를 상단에 둔다.
- 사용자별 핵심 기록을 한 행에 요약한다.
- 정정 기능은 체크인·체크아웃 중심의 현재 운영 범위를 유지한다.
- 정정 사유와 감사 기록 요구를 유지한다.

#### 계정 관리

- 활성, 초대 대기, 관리자 수를 먼저 요약한다.
- 초대 링크 생성, 복사, 비활성화 동작과 권한 범위를 유지한다.
- 부서와 역할을 명확한 텍스트로 표시한다.

#### 시스템 설정

- 출입 허용 구역, GPS 정확도, 근무 시간, 명단 동기화를 그룹화한다.
- 조회 상태와 편집 상태를 명확히 구분한다.
- 지도 편집과 시간 편집을 동시에 활성화하지 않는 기존 동작을 유지한다.
- 저장 버튼은 변경 가능한 역할에만 활성화한다.

관리자 E2E가 사용하는 `관리자 대시보드` 텍스트 또는 동등한 접근 가능한 이름을 제거하지 않는다.

## 8. 컴포넌트 체계

### 8.1 로컬 표현 컴포넌트

순수 시각 요소는 프로젝트가 소유한다.

| 권장 컴포넌트 | 책임 |
| --- | --- |
| `MobileAppShell` | 430px 앱 셸, safe area, 배경 |
| `BrandHeader` | 로고, 사용자 또는 운영자 정보 |
| `GlassCard` | 글래스 표면, 경계, 반경, 그림자 |
| `PrimaryActionButton` | 주요 행동의 시각 상태와 로딩 표현 |
| `StatusBadge` | 준비, 성공, 주의, 오류 텍스트 상태 |
| `MetricCard` | 관리자 지표 |
| `EmployeeListItem` | 관리자 사람 중심 목록 |
| `BottomNavigation` | 관리자 URL 기반 네 구역 링크 |

이 컴포넌트들은 업무 데이터를 계산하지 않고 전달받은 값만 표현한다.

### 8.2 Radix 기반 래퍼

Radix를 직접 여러 화면에서 조립하지 않고 프로젝트용 얇은 래퍼를 둔다.

| 권장 래퍼 | 기반 프리미티브 |
| --- | --- |
| `AppDialog` | `Dialog` |
| `ConfirmDialog` | `AlertDialog` |
| `AppCollapsible` | `Collapsible` 또는 `Accordion` |
| `AppPopover` | `Popover` |
| `AppSelect` | `Select` |
| `AppTooltip` | `Tooltip` |

래퍼는 디자인 토큰과 접근 가능한 기본 구조만 제공한다. 도메인 상태와 API 호출을 소유하지 않는다.

### 8.3 기존 컴포넌트 매핑

| 기존 영역 | 리디자인 방식 |
| --- | --- |
| `AttendanceActionPanel` | 로직 유지, 카드와 버튼 표현만 새 컴포넌트로 교체 |
| `CheckDateStatusRow` | 날짜·진행률 요약으로 재배치, 상태 계산 유지 |
| `UserLocationMap` | 접힘 가능한 상세 영역에 배치, 지도 로직 유지 |
| `AllPeriodsDrawer` | Radix Dialog/Collapsible 후보, 행 데이터 유지 |
| `AttendanceManagementPanel` | 모바일 사람 목록으로 표현, 조회/수정 동작 유지 |
| `AdminUserManagementPanel` | 계정 목록 카드로 표현, 권한 로직 유지 |
| `AdminInviteLinkPanel` | 초대 요약과 목록으로 표현, 토큰 처리 유지 |
| `AdminSettingsPanel` | 설정 그룹 카드로 재배치, 편집·저장 로직 유지 |
| `KakaoZoneMap` | 지도 자산과 좌표 동작 유지, 주변 컨트롤만 리스타일 |
| `TimeWheelPicker` | 내부 동작 유지, 색상·간격·표면만 변경 |

## 9. 접근성 기준

- 모든 터치 대상은 최소 `44 × 44px`이다.
- 주요 출결 버튼은 최소 높이 `58px`, 권장 높이 `62px`이다.
- 키보드 포커스는 항상 보이는 `:focus-visible` 스타일을 제공한다.
- 상태는 색상, 텍스트와 필요 시 아이콘을 함께 사용한다.
- Dialog는 열릴 때 제목으로 이동하고 닫힐 때 트리거로 포커스를 돌려준다.
- 본문 텍스트와 배경은 WCAG AA 대비를 목표로 한다.
- 반투명 표면 위 텍스트는 실제 합성 배경에서도 대비를 확인한다.
- `prefers-reduced-motion: reduce`에서는 이동 애니메이션을 제거한다.
- `backdrop-filter`가 지원되지 않아도 불투명한 대체 표면으로 읽을 수 있어야 한다.
- 시스템 글자 확대 시 가로 스크롤 없이 주요 행동을 완료할 수 있어야 한다.

## 10. 모션 및 피드백

- 마이크로 전환: `120–180ms`
- 패널 열기·닫기: 최대 `240ms`
- 애니메이션 속성은 `opacity`와 `transform`을 우선한다.
- 출결 성공 전에는 성공 애니메이션을 표시하지 않는다.
- 로딩 애니메이션이 버튼 텍스트나 상태 사유를 대체하지 않는다.
- 장시간 반복되는 장식 애니메이션과 배경 움직임은 사용하지 않는다.

## 11. 브랜드와 이미지 자산

- 운영 구현에서는 `docs/tmp/assets/juyon-snc-logo.png`의 실제 로고를 `public/brand/` 아래의 명확한 파일명으로 옮겨 사용한다.
- 로고 비율을 변경하거나 CSS 도형, 텍스트 또는 임의 SVG로 재제작하지 않는다.
- 흰색 배경이 필요한 로고는 흰색 logo plate 위에 표시한다.
- 로고의 네이비를 브랜드 기준으로 사용하고 임의의 보라색으로 변경하지 않는다.
- Kakao 로그인 자산은 공식 형태와 색상을 유지한다.
- 지도와 위치 UI에 가짜 지도 이미지나 CSS로 그린 마커를 사용하지 않는다.

## 12. 향후 구현 파일 경계

리디자인 구현 시 주로 변경할 영역:

- `app/globals.css`
- `app/layout.tsx`
- `app/login/**`
- `app/(protected)/layout.tsx`
- `app/(protected)/dashboard/page.tsx`
- `app/(protected)/admin/page.tsx`
- `components/**`의 JSX 표현 계층
- 새 `components/ui/**` 표현·Radix 래퍼
- `public/brand/**` 브랜드 자산

리디자인만으로 변경하지 않을 영역:

- `lib/**`의 도메인·저장소·인증 로직
- `app/api/**`
- `supabase/**`
- 환경 변수와 배포 설정

표현과 로직이 한 파일에 함께 있는 경우 이벤트 핸들러와 상태 전이는 유지하고 마크업과 클래스만 단계적으로 교체한다. 구조 분리가 필요하면 동일 동작을 검증하는 테스트를 먼저 마련한다.

## 13. 권장 적용 순서

1. 현재 로그인, 사용자, 관리자 화면의 기준 스크린샷과 E2E 동작을 확보한다.
2. `app/globals.css`의 기존 색상 변수를 이 문서의 의미 기반 토큰으로 매핑한다.
3. 앱 셸, 헤더, 글래스 카드, 버튼, 배지 같은 순수 표현 컴포넌트를 만든다.
4. 사용자 출결 화면을 모바일 구조로 바꾸고 출결 로직 회귀 테스트를 수행한다.
5. 관리자 네 구역을 URL 기반 하단 내비게이션과 모바일 목록으로 바꾼다.
6. Dialog, Collapsible, Popover 등 필요한 곳에만 Radix 래퍼를 적용한다.
7. 일반 UI 아이콘을 Lucide로 교체하되 브랜드 자산은 유지한다.
8. `360 × 800`, `390 × 844`, `430 × 932`에서 시각·상호작용 QA를 수행한다.
9. 타입 검사, 린트, 빌드와 Playwright E2E를 통과시킨다.

각 단계는 독립적으로 되돌릴 수 있어야 하며, 여러 화면과 인터랙션 프리미티브를 한 번에 교체하지 않는다.

## 14. 검수 기준

### 14.1 시각

- 목업 3의 블루 캔버스, 글래스 표면, 네이비 주요 카드가 일관되게 적용된다.
- 실제 주연SNC 로고가 왜곡 없이 표시된다.
- 첫 화면에서 현재 상태와 다음 행동이 가장 강하게 보인다.
- 주요 버튼이 모바일 첫 화면의 엄지 영역에 있다.
- 관리자 화면에 모바일 가로 스크롤 표가 없다.

### 14.2 기능

- Kakao 초대 로그인과 개발 모드 역할 로그인이 기존과 동일하게 동작한다.
- 부서별 이벤트 노출 차이가 유지된다.
- 위치, GPS 정확도, 시간 창과 선행 이벤트 검증이 유지된다.
- 출결 성공은 서버 저장 성공 이후에만 표시된다.
- 관리자 역할별 조회·수정 범위가 유지된다.
- Google Sheet와 Supabase 데이터 경로가 변경되지 않는다.

### 14.3 기술

- `npm run typecheck` 통과
- `npm run lint` 통과
- `npm run build` 통과
- 핵심 Playwright 시나리오 통과
- 브라우저 콘솔 오류 없음
- `320px` 이상에서 가로 오버플로 없음
- 키보드만으로 Dialog, Select, Collapsible을 사용할 수 있음
- 시스템 동작 감소 설정에서 불필요한 모션 없음

## 15. 알려진 현재 위험

`IMPLEMENTATION_AUDIT.md`에 기록된 다음 항목은 리디자인이 해결하거나 숨겨서는 안 된다.

- Demo 모드의 부서별 시간 설정 적용 불일치
- Supabase 시간 설정의 실제 환경 검증 필요
- 현재 E2E 테스트 하네스의 신뢰성 문제

이 문제들은 UI 리디자인과 분리해서 수정하며, 디자인 변경의 성공 기준으로 잘못 간주하지 않는다.
